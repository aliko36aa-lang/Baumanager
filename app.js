const SU="https://mqgnuvsgrdsjuxaogpnd.supabase.co";
const SK="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1xZ251dnNncmRzanV4YW9ncG5kIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQyMjM5NDIsImV4cCI6MjA4OTc5OTk0Mn0.8C0EaWIitlhWtU_CkZ9RfnsJK8gEs5Lj_srrQTkJwe0";
const sb=window.supabase.createClient(SU,SK);
let me=null;
let settings={firma:'',adresse:'',telefon:'',email:'',mwst:19,zahlungsziel:14,iban:'',bank:'',ustid:'',steuernr:''};
async function dL(){const e=document.getElementById('le').value.trim(),p=document.getElementById('lp').value;const er=document.getElementById('lerr');er.textContent='';if(!e||!p){er.textContent='Bitte alle Felder ausfüllen.';return;}const b=document.querySelector('.bl');b.textContent='…';b.disabled=true;const{data,error}=await sb.auth.signInWithPassword({email:e,password:p});b.textContent='Anmelden →';b.disabled=false;if(error){er.textContent='❌ '+(error.message.includes('Invalid')?'Falsche Zugangsdaten.':error.message);return;}sA(data.user);}
async function dLo(){await sb.auth.signOut();location.reload();}
function sA(u){me=u;document.getElementById('ls').style.display='none';document.getElementById('app').style.display='block';document.getElementById('tb').style.display='flex';document.getElementById('ue').textContent=u.email;document.getElementById('ua').textContent=u.email[0].toUpperCase();loadSettings();go('dash');}
const titles={dash:'Dashboard',kunden:'Kunden',proj:'Projekte',termine:'Termine',angebote:'Angebote',mit:'Mitarbeiter',std:'Stunden',mat:'Material',rech:'Rechnungen',einst:'Einstellungen'};
function go(p){document.querySelectorAll('.ni').forEach(el=>el.classList.toggle('active',el.dataset.p===p));document.getElementById('tt').textContent=titles[p]||p;cSB();({dash:pgDash,kunden:pgKunden,proj:pgProj,termine:pgTermine,angebote:pgAngebote,mit:pgMit,std:pgStd,mat:pgMat,rech:pgRech,einst:pgEinst})[p]?.();}
function tSB(){document.getElementById('sb').classList.toggle('open');document.getElementById('ov').classList.toggle('show');}
function cSB(){document.getElementById('sb').classList.remove('open');document.getElementById('ov').classList.remove('show');}
function sC(h){document.getElementById('ct').innerHTML=h;}
function ld(){sC('<div class="ld">⏳ Wird geladen…</div>');}
function oM(h){document.getElementById('mi').innerHTML=h;document.getElementById('mo').classList.add('open');}
function cM(){document.getElementById('mo').classList.remove('open');}
async function loadSettings(){try{const{data}=await sb.from('einstellungen').select('*').eq('user_id',me.id).single();if(data)settings={...settings,...data};}catch(e){}}
async function saveSettings(){const o={user_id:me.id,firma:document.getElementById('s-firma').value,adresse:document.getElementById('s-adr').value,telefon:document.getElementById('s-tel').value,email:document.getElementById('s-em').value,mwst:parseFloat(document.getElementById('s-mwst').value)||19,zahlungsziel:parseInt(document.getElementById('s-ziel').value)||14,iban:document.getElementById('s-iban').value,bank:document.getElementById('s-bank').value,ustid:document.getElementById('s-ustid').value,steuernr:document.getElementById('s-steuernr').value};const{error}=await sb.from('einstellungen').upsert(o,{onConflict:'user_id'});if(error){alert('Fehler: '+error.message);return;}settings={...settings,...o};alert('✅ Einstellungen gespeichert!');pgEinst();}
async function pgDash(){ld();try{const[r1,r2,r3,r4]=await Promise.all([sb.from('projekte').select('*'),sb.from('kunden').select('*'),sb.from('termine').select('*'),sb.from('rechnungen').select('betrag,status')]);const proj=r1.data||[],ku=r2.data||[],t=r3.data||[],r=r4.data||[];const offen=r.filter(x=>x.status==='offen').reduce((s,x)=>s+(parseFloat(x.betrag)||0),0);const heute=new Date().toISOString().split('T')[0];const hT=t.filter(x=>x.datum===heute);sC(`<div class="ph"><div><div class="pt">Guten Tag! 👋</div><div class="ps">Übersicht deiner Baustellen</div></div></div><div class="sts"><div class="st"><div class="si">🏗️</div><div class="sv">${proj.length}</div><div class="sl">Projekte</div></div><div class="st g"><div class="si">👥</div><div class="sv">${ku.length}</div><div class="sl">Kunden</div></div><div class="st o"><div class="si">📅</div><div class="sv">${hT.length}</div><div class="sl">Heute Termine</div></div><div class="st pu"><div class="si">💶</div><div class="sv">€${offen.toFixed(0)}</div><div class="sl">Offene Rechnungen</div></div></div><div class="sh">Aktuelle Projekte</div>${!proj.length?'<div class="em"><div class="em-i">🏗️</div><div class="em-t">Noch keine Projekte</div></div>':`<div class="gr">${proj.slice(0,6).map(p=>`<div class="ca" onclick="pgProjDetail('${p.id}')"><div class="ca-t">${p.name||'–'}</div><div class="ca-s">${p.kunde||p.kunde_name||'–'}</div>${p.adresse?`<div style="font-size:11px;color:var(--muted);margin-top:6px">📍 ${p.adresse}</div>`:''}<div class="ca-f"><span class="bd bd-g">${p.status||'Aktiv'}</span></div></div>`).join('')}</div>`}`);}catch(e){sC(`<div class="em"><div class="em-i">⚠️</div><div class="em-t">Fehler: ${e.message}</div></div>`);}}
async function pgKunden(){ld();try{const{data}=await sb.from('kunden').select('*').order('created_at',{ascending:false});sC(`<div class="ph"><div><div class="pt">👥 Kunden</div><div class="ps">${(data||[]).length} Kunde(n)</div></div><button class="bp" onclick="mKunde()">＋ Neuer Kunde</button></div>${!(data&&data.length)?'<div class="em"><div class="em-i">👥</div><div class="em-t">Noch keine Kunden</div></div>':`<div class="gr">${data.map(k=>`<div class="ca" onclick="pgKundeDetail('${k.id}')"><div style="display:flex;align-items:center;gap:10px"><div style="width:40px;height:40px;background:var(--pl);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:16px;font-weight:800;color:var(--p)">${(k.name||'?')[0].toUpperCase()}</div><div><div class="ca-t">${k.name||'–'}</div><div class="ca-s">${k.telefon||k.email||'–'}</div></div></div>${k.adresse?`<div style="font-size:11px;color:var(--muted);margin-top:8px">📍 ${k.adresse}</div>`:''}<div class="ca-f"><span class="bd bd-b">Details →</span></div></div>`).join('')}</div>`}`);}catch(e){sC(`<div class="em"><div class="em-i">⚠️</div><div class="em-t">${e.message}</div></div>`);}}
function mKunde(k={}){oM(`<div class="mt">${k.id?'Kunde bearbeiten':'👥 Neuer Kunde'}</div><div class="fg"><label>NAME *</label><input id="f-n" value="${k.name||''}" placeholder="Frank Müller"></div><div class="fr"><div class="fg"><label>TELEFON</label><input id="f-tel" value="${k.telefon||''}" placeholder="+49 176 ..."></div><div class="fg"><label>E-MAIL</label><input id="f-em" value="${k.email||''}" placeholder="info@..."></div></div><div class="fg"><label>ADRESSE</label><input id="f-adr" value="${k.adresse||''}" placeholder="Musterstr. 1, Berlin"></div><div class="fg"><label>NOTIZ</label><textarea id="f-not" rows="2">${k.notiz||''}</textarea></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveKunde('${k.id||''}')">💾 Speichern</button></div>`);}
async function saveKunde(id){const n=document.getElementById('f-n').value.trim();if(!n){alert('Bitte Name eingeben!');return;}const o={name:n,telefon:document.getElementById('f-tel').value,email:document.getElementById('f-em').value,adresse:document.getElementById('f-adr').value,notiz:document.getElementById('f-not').value,user_id:me.id};const{error}=id?await sb.from('kunden').update(o).eq('id',id):await sb.from('kunden').insert(o);if(error){alert('Fehler: '+error.message);return;}cM();pgKunden();}
async function delKunde(id){if(!confirm('Kunde wirklich löschen?'))return;const{error}=await sb.from('kunden').delete().eq('id',id);if(error){alert('Fehler: '+error.message);return;}pgKunden();}
async function pgKundeDetail(id){ld();const[{data:k},{data:proj}]=await Promise.all([sb.from('kunden').select('*').eq('id',id).single(),sb.from('projekte').select('*').eq('kunden_id',id)]);const mapQ=encodeURIComponent(k?.adresse||'Berlin');sC(`<button class="bk" onclick="pgKunden()">← Zurück</button><div class="ph"><div><div class="pt">${k?.name||'Kunde'}</div><div class="ps">${k?.telefon||''}</div></div><div style="display:flex;gap:8px"><button class="bp" onclick='mKunde(${JSON.stringify(k).replace(/'/g,"&#39;")})'>✏️ Bearbeiten</button><button class="bp" style="background:var(--red)" onclick="delKunde('${id}')">🗑️</button></div></div><div class="info-grid"><div class="info-box"><div class="info-label">TELEFON</div><div class="info-val">${k?.telefon||'–'}</div></div><div class="info-box"><div class="info-label">E-MAIL</div><div class="info-val" style="font-size:12px">${k?.email||'–'}</div></div><div class="info-box" style="grid-column:1/-1"><div class="info-label">ADRESSE</div><div class="info-val">${k?.adresse||'–'}</div></div></div>${k?.adresse?`<div class="map-wrap"><iframe src="https://maps.google.com/maps?q=${mapQ}&output=embed&z=15" allowfullscreen></iframe></div>`:''}<div class="sh">🏗️ Projekte</div>${!(proj&&proj.length)?'<div class="em" style="padding:20px"><div class="em-t">Keine Projekte</div></div>':`<div class="gr">${proj.map(p=>`<div class="ca" onclick="pgProjDetail('${p.id}')"><div class="ca-t">${p.name}</div></div>`).join('')}</div>`}`);}
async function pgProj(){ld();try{const{data}=await sb.from('projekte').select('*').order('created_at',{ascending:false});sC(`<div class="ph"><div><div class="pt">🏗️ Projekte</div><div class="ps">${(data||[]).length} Projekt(e)</div></div><button class="bp" onclick="mProj()">＋ Neues Projekt</button></div>${!(data&&data.length)?'<div class="em"><div class="em-i">🏗️</div><div class="em-t">Noch keine Projekte</div></div>':`<div class="gr">${data.map(p=>`<div class="ca" onclick="pgProjDetail('${p.id}')"><div class="ca-t">${p.name||'–'}</div><div class="ca-s">${p.kunde||p.kunde_name||'–'}</div>${p.adresse?`<div style="font-size:11px;color:var(--muted);margin-top:5px">📍 ${p.adresse}</div>`:''}<div class="ca-f"><span class="bd bd-g">${p.status||'Aktiv'}</span></div></div>`).join('')}</div>`}`);}catch(e){sC(`<div class="em"><div class="em-i">⚠️</div><div class="em-t">${e.message}</div></div>`);}}
async function mProj(){const{data:ku}=await sb.from('kunden').select('id,name');oM(`<div class="mt">🏗️ Neues Projekt</div><div class="fg"><label>PROJEKTNAME *</label><input id="f-n" placeholder="z.B. Baustelle Berlin"></div><div class="fg"><label>KUNDE</label><select id="f-ku"><option value="">– Kein Kunde –</option>${(ku||[]).map(k=>`<option value="${k.id}" data-n="${k.name}">${k.name}</option>`).join('')}</select></div><div class="fg"><label>ADRESSE</label><input id="f-adr" placeholder="Musterstraße 1, Berlin"></div><div class="fg"><label>BESCHREIBUNG</label><textarea id="f-desc" rows="3"></textarea></div><div class="fr"><div class="fg"><label>START</label><input id="f-start" type="date"></div><div class="fg"><label>ENDE</label><input id="f-end" type="date"></div></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveProj()">💾 Speichern</button></div>`);}
async function saveProj(){const n=document.getElementById('f-n').value.trim();if(!n){alert('Projektname fehlt!');return;}const s=document.getElementById('f-ku');const ki=s.value||null,kn=ki?s.options[s.selectedIndex].dataset.n:'';const{error}=await sb.from('projekte').insert({name:n,kunden_id:ki,kunde_name:kn,kunde:kn,adresse:document.getElementById('f-adr').value,beschreibung:document.getElementById('f-desc').value,datum_start:document.getElementById('f-start').value||null,datum_end:document.getElementById('f-end').value||null,autor_id:me.id});if(error){alert('Fehler: '+error.message);return;}cM();pgProj();}
async function delProj(id){if(!confirm('Projekt wirklich löschen?'))return;const{error}=await sb.from('projekte').delete().eq('id',id);if(error){alert('Fehler: '+error.message);return;}pgProj();}
async function pgProjDetail(id){ld();const[{data:p},{data:bt},{data:mat},{data:std},{data:ang}]=await Promise.all([sb.from('projekte').select('*').eq('id',id).single(),sb.from('bautagebuch').select('*').eq('projekt_id',id).order('datum',{ascending:false}),sb.from('material').select('*').eq('projekt_id',id),sb.from('stunden').select('*').eq('projekt_id',id),sb.from('angebote').select('*').eq('projekt_id',id)]);const mapQ=encodeURIComponent(p?.adresse||'Berlin');sC(`<button class="bk" onclick="pgProj()">← Zurück</button><div class="ph"><div><div class="pt">${p?.name||'Projekt'}</div><div class="ps">${p?.kunde||p?.kunde_name||''} ${p?.adresse?'· '+p.adresse:''}</div></div><button class="bp" style="background:var(--red)" onclick="delProj('${id}')">🗑️ Löschen</button></div>${p?.adresse?`<div class="map-wrap"><iframe src="https://maps.google.com/maps?q=${mapQ}&output=embed&z=15" allowfullscreen></iframe></div>`:''}<div class="sh">📸 Fotos</div><div class="photo-up" onclick="document.getElementById('fi').click()"><input type="file" id="fi" accept="image/*" multiple style="display:none" onchange="upFotos('${id}')"><div style="font-size:24px;margin-bottom:6px">📷</div><div style="font-weight:700;font-size:13px">Fotos hinzufügen</div></div><div id="fp" class="photo-prev"></div><div class="sh">📋 Bautagebuch (${(bt||[]).length})</div><div class="tc"><div class="th"><span class="th-t">Einträge</span><button class="bp" style="padding:7px 12px;font-size:12px" onclick="mBt('${id}')">＋ Eintrag</button></div><table><thead><tr><th>Datum</th><th>Beschreibung</th><th>Wetter</th><th></th></tr></thead><tbody>${!(bt&&bt.length)?'<tr><td colspan="4" style="text-align:center;padding:20px;color:var(--muted)">Keine Einträge</td></tr>':bt.map(e=>`<tr><td>${e.datum||'–'}</td><td>${e.beschreibung||'–'}</td><td>${e.wetter||'–'}</td><td style="white-space:nowrap"><button onclick="loadAndExportBt('${e.id}','${p?.name||''}')" style="background:none;border:none;cursor:pointer;font-size:16px">📄</button><button onclick="delBt('${e.id}','${id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div><div class="sh">📄 Angebote (${(ang||[]).length})</div><div class="tc"><div class="th"><span class="th-t">Angebote</span><button class="bp" style="padding:7px 12px;font-size:12px" onclick="mAng('${id}','${p?.kunde||p?.kunde_name||''}')">＋ Angebot</button></div><table><thead><tr><th>Nr.</th><th>Titel</th><th>Betrag</th><th>Status</th><th></th></tr></thead><tbody>${!(ang&&ang.length)?'<tr><td colspan="5" style="text-align:center;padding:20px;color:var(--muted)">Keine Angebote</td></tr>':ang.map(a=>`<tr><td style="font-family:monospace;font-size:11px">${a.nummer||'–'}</td><td>${a.titel||'–'}</td><td><b>€${parseFloat(a.gesamt||0).toFixed(2)}</b></td><td><span class="bd ${a.status==='angenommen'?'bd-g':a.status==='abgelehnt'?'bd-r':'bd-o'}">${a.status||'offen'}</span></td><td><button onclick="delAng('${a.id}','${id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div><div class="sh">🧱 Material (${(mat||[]).length})</div><div class="tc"><div class="th"><span class="th-t">Material</span><button class="bp" style="padding:7px 12px;font-size:12px" onclick="mMat2('${id}')">＋ Material</button></div><table><thead><tr><th>Material</th><th>Menge</th><th>Preis</th><th></th></tr></thead><tbody>${!(mat&&mat.length)?'<tr><td colspan="4" style="text-align:center;padding:20px;color:var(--muted)">Kein Material</td></tr>':mat.map(m=>`<tr><td><b>${m.name||'–'}</b></td><td>${m.menge||'–'}</td><td>${m.preis?'€'+parseFloat(m.preis).toFixed(2):'–'}</td><td style="white-space:nowrap"><button onclick="mMatEdit('${m.id}','${m.name||''}','${m.menge||''}','${m.preis||''}')" style="background:none;border:none;cursor:pointer;font-size:16px">✏️</button><button onclick="delMat('${m.id}','${id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div><div class="sh">⏱️ Stunden (${(std||[]).length})</div><div class="tc"><div class="th"><span class="th-t">Stunden</span><button class="bp" style="padding:7px 12px;font-size:12px" onclick="mStd2('${id}')">＋ Stunden</button></div><table><thead><tr><th>Datum</th><th>Mitarbeiter</th><th>Stunden</th><th></th></tr></thead><tbody>${!(std&&std.length)?'<tr><td colspan="4" style="text-align:center;padding:20px;color:var(--muted)">Keine Stunden</td></tr>':std.map(s=>`<tr><td>${s.datum||'–'}</td><td>${s.mitarbeiter_name||'–'}</td><td><b>${s.stunden||0}h</b></td><td><button onclick="delStd('${s.id}','${id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div>`);}
async function loadAndExportBt(btId,projektName){const{data}=await sb.from('bautagebuch').select('*').eq('id',btId).single();if(data)exportBtPDF(data,projektName,settings.firma||'');}

async function delBt(id,pid){if(!confirm('Eintrag löschen?'))return;await sb.from('bautagebuch').delete().eq('id',id);pgProjDetail(pid);}
async function delMat(id,pid){if(!confirm('Material löschen?'))return;await sb.from('material').delete().eq('id',id);pgProjDetail(pid);}
async function delStd(id,pid){if(!confirm('Stunden löschen?'))return;await sb.from('stunden').delete().eq('id',id);pgProjDetail(pid);}
async function delAng(id,pid){if(!confirm('Angebot löschen?'))return;await sb.from('angebote').delete().eq('id',id);pgProjDetail(pid);}
function mMatEdit(id,name,menge,preis){oM(`<div class="mt">🧱 Material bearbeiten</div><div class="fg"><label>MATERIALNAME *</label><input id="f-n" value="${name}"></div><div class="fr"><div class="fg"><label>MENGE</label><input id="f-m" value="${menge}"></div><div class="fg"><label>PREIS (€)</label><input id="f-pr" type="number" step="0.01" value="${preis}"></div></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveMatEdit('${id}')">💾 Speichern</button></div>`);}
async function saveMatEdit(id){const n=document.getElementById('f-n').value.trim();if(!n){alert('Name fehlt!');return;}const{error}=await sb.from('material').update({name:n,menge:document.getElementById('f-m').value,preis:parseFloat(document.getElementById('f-pr').value)||null}).eq('id',id);if(error){alert('Fehler: '+error.message);return;}cM();pgMat();}
async function pgTermine(){ld();try{const heute=new Date();const[{data},{data:proj}]=await Promise.all([sb.from('termine').select('*').order('datum'),sb.from('projekte').select('id,name')]);const yr=heute.getFullYear(),mo=heute.getMonth();const firstDay=new Date(yr,mo,1).getDay();const dim=new Date(yr,mo+1,0).getDate();const monate=['Januar','Februar','März','April','Mai','Juni','Juli','August','September','Oktober','November','Dezember'];let cc='';const pad=n=>String(n).padStart(2,'0');for(let i=0;i<(firstDay===0?6:firstDay-1);i++)cc+=`<div class="cal-day" style="background:#fafafa"></div>`;for(let d=1;d<=dim;d++){const ds=`${yr}-${pad(mo+1)}-${pad(d)}`;const de=(data||[]).filter(t=>t.datum===ds);const it=ds===heute.toISOString().split('T')[0];cc+=`<div class="cal-day${it?' today':''}" onclick='mTermin("${ds}",${JSON.stringify(proj||[]).replace(/"/g,"&quot;")})'><div class="cal-dn${it?' cur':''}">${d}</div>${de.slice(0,2).map(e=>`<div class="cal-ev">${e.titel||'Termin'}</div>`).join('')}</div>`;}const up=(data||[]).filter(t=>t.datum>=heute.toISOString().split('T')[0]).slice(0,10);sC(`<div class="ph"><div><div class="pt">📅 Termine</div><div class="ps">${(data||[]).length} Termin(e)</div></div><button class="bp" onclick='mTermin("",${JSON.stringify(proj||[]).replace(/'/g,"&#39;")})'>＋ Neuer Termin</button></div><div class="cal"><div class="cal-head"><div style="font-size:15px;font-weight:700">📅 ${monate[mo]} ${yr}</div></div><div class="cal-grid"><div class="cal-day-h">Mo</div><div class="cal-day-h">Di</div><div class="cal-day-h">Mi</div><div class="cal-day-h">Do</div><div class="cal-day-h">Fr</div><div class="cal-day-h">Sa</div><div class="cal-day-h">So</div>${cc}</div></div><div class="sh">Kommende Termine</div><div class="tc"><table><thead><tr><th>Datum</th><th>Zeit</th><th>Titel</th><th>Status</th><th></th></tr></thead><tbody>${!up.length?'<tr><td colspan="5" style="text-align:center;padding:20px;color:var(--muted)">Keine Termine</td></tr>':up.map(t=>`<tr><td>${t.datum||'–'}</td><td>${t.uhrzeit||'–'}</td><td><b>${t.titel||'–'}</b></td><td><span class="bd ${t.status==='erledigt'?'bd-g':t.status==='abgesagt'?'bd-r':'bd-b'}">${t.status||'geplant'}</span></td><td><button onclick="delTermin('${t.id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div>`);}catch(e){sC(`<div class="em"><div class="em-i">⚠️</div><div class="em-t">${e.message}</div></div>`);}}
function mTermin(datum,proj){oM(`<div class="mt">📅 Neuer Termin</div><div class="fg"><label>TITEL *</label><input id="f-t" placeholder="z.B. Wartungsarbeiten"></div><div class="fg"><label>PROJEKT</label><select id="f-p"><option value="">– Kein Projekt –</option>${(proj||[]).map(p=>`<option value="${p.id}">${p.name}</option>`).join('')}</select></div><div class="fr"><div class="fg"><label>DATUM</label><input id="f-d" type="date" value="${datum||new Date().toISOString().split('T')[0]}"></div><div class="fg"><label>UHRZEIT</label><input id="f-u" type="time" value="08:00"></div></div><div class="fg"><label>STATUS</label><select id="f-s"><option value="geplant">Geplant</option><option value="erledigt">Erledigt</option><option value="abgesagt">Abgesagt</option></select></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveTermin()">💾 Speichern</button></div>`);}
async function saveTermin(){const t=document.getElementById('f-t').value.trim();if(!t){alert('Titel fehlt!');return;}const{error}=await sb.from('termine').insert({titel:t,projekt_id:document.getElementById('f-p').value||null,datum:document.getElementById('f-d').value,uhrzeit:document.getElementById('f-u').value,status:document.getElementById('f-s').value,user_id:me.id});if(error){alert('Fehler: '+error.message);return;}cM();pgTermine();}
async function delTermin(id){if(!confirm('Termin löschen?'))return;await sb.from('termine').delete().eq('id',id);pgTermine();}
async function pgAngebote(){ld();try{const[{data},{data:proj}]=await Promise.all([sb.from('angebote').select('*').order('created_at',{ascending:false}),sb.from('projekte').select('id,name')]);sC(`<div class="ph"><div><div class="pt">📄 Angebote</div><div class="ps">${(data||[]).length} Angebot(e)</div></div><button class="bp" onclick='mAng("","",${JSON.stringify(proj||[]).replace(/'/g,"&#39;")})'>＋ Neues Angebot</button></div><div class="tc"><table><thead><tr><th>Nr.</th><th>Datum</th><th>Titel</th><th>Betrag</th><th>Status</th><th></th></tr></thead><tbody>${!(data&&data.length)?'<tr><td colspan="6"><div class="em" style="padding:30px"><div class="em-i">📄</div><div class="em-t">Keine Angebote</div></div></td></tr>':data.map(a=>`<tr><td style="font-family:monospace;font-size:11px">${a.nummer||'–'}</td><td>${a.datum||'–'}</td><td><b>${a.titel||'–'}</b></td><td><b>€${parseFloat(a.gesamt||0).toFixed(2)}</b></td><td><span class="bd ${a.status==='angenommen'?'bd-g':a.status==='abgelehnt'?'bd-r':'bd-o'}">${a.status||'offen'}</span></td><td><button onclick="delAngD('${a.id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div>`);}catch(e){sC(`<div class="em"><div class="em-i">⚠️</div><div class="em-t">${e.message}</div></div>`);}}
function mAng(pid,kn,proj){const nr='A-'+new Date().getFullYear()+'-'+String(Math.floor(Math.random()*9000)+1000);oM(`<div class="mt">📄 Angebot erstellen</div><div class="fr"><div class="fg"><label>ANGEBOTSNR.</label><input id="f-nr" value="${nr}"></div><div class="fg"><label>DATUM</label><input id="f-d" type="date" value="${new Date().toISOString().split('T')[0]}"></div></div><div class="fg"><label>TITEL *</label><input id="f-t" placeholder="z.B. Sanierungsarbeiten"></div><div class="fg"><label>PROJEKT</label><select id="f-p"><option value="">–</option>${(proj||[]).map(p=>`<option value="${p.id}"${p.id===pid?' selected':''}>${p.name}</option>`).join('')}</select></div><div class="fg"><label>KUNDE</label><input id="f-ku" value="${kn||''}" placeholder="Kundenname"></div><div class="fg"><label>POSITIONEN</label><div id="pos-wrap"><div class="offer-row"><input placeholder="Leistung" class="pos-name"><input placeholder="Preis €" type="number" class="pos-price price-inp" oninput="calcTotal()"></div></div><button type="button" onclick="addPos()" style="margin-top:6px;padding:6px 12px;background:var(--pl);color:var(--pd);border:none;border-radius:7px;font-size:12px;font-weight:700;cursor:pointer;font-family:inherit">＋ Position</button><div class="offer-total">Gesamt: €<span id="tot">0.00</span></div></div><div class="fg"><label>STATUS</label><select id="f-s"><option value="offen">Offen</option><option value="angenommen">Angenommen</option><option value="abgelehnt">Abgelehnt</option></select></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveAng()">💾 Speichern</button></div>`);}
function addPos(){const w=document.getElementById('pos-wrap');const r=document.createElement('div');r.className='offer-row';r.innerHTML='<input placeholder="Leistung" class="pos-name"><input placeholder="Preis €" type="number" class="pos-price price-inp" oninput="calcTotal()">';w.appendChild(r);}
function calcTotal(){let t=0;document.querySelectorAll('.pos-price').forEach(p=>t+=parseFloat(p.value)||0);document.getElementById('tot').textContent=t.toFixed(2);}
async function saveAng(){const t=document.getElementById('f-t').value.trim();if(!t){alert('Titel fehlt!');return;}const ns=document.querySelectorAll('.pos-name'),ps=document.querySelectorAll('.pos-price');const pos=[];let total=0;ns.forEach((n,i)=>{if(n.value.trim()){const pr=parseFloat(ps[i].value)||0;pos.push({name:n.value,preis:pr});total+=pr;}});const{error}=await sb.from('angebote').insert({nummer:document.getElementById('f-nr').value,titel:t,projekt_id:document.getElementById('f-p').value||null,kunde:document.getElementById('f-ku').value,datum:document.getElementById('f-d').value,positionen:JSON.stringify(pos),gesamt:total,status:document.getElementById('f-s').value,user_id:me.id});if(error){alert('Fehler: '+error.message);return;}cM();pgAngebote();}
async function delAngD(id){if(!confirm('Angebot löschen?'))return;await sb.from('angebote').delete().eq('id',id);pgAngebote();}
async function pgMit(){ld();const{data}=await sb.from('profiles').select('*');sC(`<div class="ph"><div><div class="pt">👷 Mitarbeiter</div></div></div>${!(data&&data.length)?'<div class="em"><div class="em-i">👷</div><div class="em-t">Noch keine Mitarbeiter</div></div>':`<div class="gr">${data.map(m=>`<div class="ca"><div style="display:flex;align-items:center;gap:10px"><div style="width:42px;height:42px;background:var(--pl);border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:17px;font-weight:800;color:var(--p)">${(m.full_name||m.email||'?')[0].toUpperCase()}</div><div><div class="ca-t">${m.full_name||'Unbekannt'}</div><div class="ca-s">${m.email||'–'}</div></div></div><div class="ca-f"><span class="bd bd-b">${m.rolle||'Mitarbeiter'}</span></div></div>`).join('')}</div>`}`);}
async function pgStd(){ld();try{const[{data},{data:proj}]=await Promise.all([sb.from('stunden').select('*').order('datum',{ascending:false}),sb.from('projekte').select('id,name')]);const total=(data||[]).reduce((s,r)=>s+(parseFloat(r.stunden)||0),0);sC(`<div class="ph"><div><div class="pt">⏱️ Stunden</div><div class="ps">Gesamt: ${total.toFixed(1)}h</div></div><button class="bp" onclick='mStdAllg(${JSON.stringify(proj||[]).replace(/'/g,"&#39;")})'>＋ Eintragen</button></div><div class="tc"><table><thead><tr><th>Datum</th><th>Mitarbeiter</th><th>Projekt</th><th>Stunden</th><th></th></tr></thead><tbody>${!(data&&data.length)?'<tr><td colspan="5"><div class="em" style="padding:30px"><div class="em-i">⏱️</div><div class="em-t">Keine Einträge</div></div></td></tr>':data.map(s=>`<tr><td>${s.datum||'–'}</td><td>${s.mitarbeiter_name||'–'}</td><td>${(proj||[]).find(p=>p.id===s.projekt_id)?.name||'–'}</td><td><b>${s.stunden||0}h</b></td><td><button onclick="delStdD('${s.id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div>`);}catch(e){sC(`<div class="em"><div class="em-i">⚠️</div><div class="em-t">${e.message}</div></div>`);}}
function mStd2(pid){oM(`<div class="mt">⏱️ Stunden eintragen</div><input type="hidden" id="f-p" value="${pid}"><div class="fg"><label>MITARBEITER</label><input id="f-n" placeholder="Max Mustermann"></div><div class="fg"><label>DATUM</label><input id="f-d" type="date" value="${new Date().toISOString().split('T')[0]}"></div><div class="fg"><label>STUNDEN *</label><input id="f-s" type="number" step="0.5" min="0.5" max="24" placeholder="8"></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveStd()">💾 Speichern</button></div>`);}
function mStdAllg(proj){oM(`<div class="mt">⏱️ Stunden eintragen</div><div class="fg"><label>PROJEKT</label><select id="f-p"><option value="">– Kein Projekt –</option>${(proj||[]).map(p=>`<option value="${p.id}">${p.name}</option>`).join('')}</select></div><div class="fg"><label>MITARBEITER</label><input id="f-n" placeholder="Max Mustermann"></div><div class="fg"><label>DATUM</label><input id="f-d" type="date" value="${new Date().toISOString().split('T')[0]}"></div><div class="fg"><label>STUNDEN *</label><input id="f-s" type="number" step="0.5" min="0.5" max="24" placeholder="8"></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveStd()">💾 Speichern</button></div>`);}
async function saveStd(){const s=document.getElementById('f-s').value;if(!s){alert('Stunden fehlen!');return;}const{error}=await sb.from('stunden').insert({projekt_id:document.getElementById('f-p').value||null,mitarbeiter_name:document.getElementById('f-n').value,datum:document.getElementById('f-d').value,stunden:parseFloat(s)});if(error){alert('Fehler: '+error.message);return;}cM();pgStd();}
async function delStdD(id){if(!confirm('Eintrag löschen?'))return;await sb.from('stunden').delete().eq('id',id);pgStd();}
async function pgMat(){ld();try{const[{data},{data:proj}]=await Promise.all([sb.from('material').select('*').order('created_at',{ascending:false}),sb.from('projekte').select('id,name')]);sC(`<div class="ph"><div><div class="pt">🧱 Material</div><div class="ps">${(data||[]).length} Einträge</div></div><button class="bp" onclick='mMatAllg(${JSON.stringify(proj||[]).replace(/'/g,"&#39;")})'>＋ Hinzufügen</button></div><div class="tc"><table><thead><tr><th>Material</th><th>Projekt</th><th>Menge</th><th>Preis</th><th></th></tr></thead><tbody>${!(data&&data.length)?'<tr><td colspan="5"><div class="em" style="padding:30px"><div class="em-i">🧱</div><div class="em-t">Kein Material</div></div></td></tr>':data.map(m=>`<tr><td><b>${m.name||'–'}</b></td><td>${(proj||[]).find(p=>p.id===m.projekt_id)?.name||m.projekt||'–'}</td><td>${m.menge||'–'}</td><td>${m.preis?'€'+parseFloat(m.preis).toFixed(2):'–'}</td><td style="white-space:nowrap"><button onclick="mMatEdit('${m.id}','${m.name||''}','${m.menge||''}','${m.preis||''}')" style="background:none;border:none;cursor:pointer;font-size:16px">✏️</button><button onclick="delMatD('${m.id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div>`);}catch(e){sC(`<div class="em"><div class="em-i">⚠️</div><div class="em-t">${e.message}</div></div>`);}}
function mMat2(pid){oM(`<div class="mt">🧱 Material</div><input type="hidden" id="f-p" value="${pid}"><div class="fg"><label>MATERIALNAME *</label><input id="f-n" placeholder="z.B. Zement"></div><div class="fr"><div class="fg"><label>MENGE</label><input id="f-m" placeholder="50 Säcke"></div><div class="fg"><label>PREIS (€)</label><input id="f-pr" type="number" step="0.01" placeholder="0.00"></div></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveMat()">💾 Speichern</button></div>`);}
function mMatAllg(proj){oM(`<div class="mt">🧱 Material</div><div class="fg"><label>PROJEKT</label><select id="f-p"><option value="">–</option>${(proj||[]).map(p=>`<option value="${p.id}">${p.name}</option>`).join('')}</select></div><div class="fg"><label>MATERIALNAME *</label><input id="f-n" placeholder="z.B. Zement"></div><div class="fr"><div class="fg"><label>MENGE</label><input id="f-m" placeholder="50 Säcke"></div><div class="fg"><label>PREIS (€)</label><input id="f-pr" type="number" step="0.01" placeholder="0.00"></div></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveMat()">💾 Speichern</button></div>`);}
async function saveMat(){const n=document.getElementById('f-n').value.trim();if(!n){alert('Name fehlt!');return;}const{error}=await sb.from('material').insert({projekt_id:document.getElementById('f-p').value||null,name:n,menge:document.getElementById('f-m').value,preis:parseFloat(document.getElementById('f-pr').value)||null});if(error){alert('Fehler: '+error.message);return;}cM();pgMat();}
async function delMatD(id){if(!confirm('Material löschen?'))return;await sb.from('material').delete().eq('id',id);pgMat();}
async function pgRech(){ld();try{const[{data},{data:proj}]=await Promise.all([sb.from('rechnungen').select('*').order('datum',{ascending:false}),sb.from('projekte').select('id,name')]);const paid=(data||[]).filter(r=>r.status==='bezahlt').reduce((s,r)=>s+(parseFloat(r.betrag)||0),0);const open=(data||[]).filter(r=>r.status==='offen').reduce((s,r)=>s+(parseFloat(r.betrag)||0),0);sC(`<div class="ph"><div><div class="pt">💶 Rechnungen</div><div class="ps">${(data||[]).length} Rechnung(en)</div></div><button class="bp" onclick='mRech(${JSON.stringify(proj||[]).replace(/'/g,"&#39;")})'>＋ Erstellen</button></div><div class="sts" style="grid-template-columns:repeat(auto-fill,minmax(170px,1fr))"><div class="st g"><div class="si">✅</div><div class="sv">€${paid.toFixed(0)}</div><div class="sl">Bezahlt</div></div><div class="st o"><div class="si">⏳</div><div class="sv">€${open.toFixed(0)}</div><div class="sl">Offen</div></div></div><div class="tc"><table><thead><tr><th>Nr.</th><th>Datum</th><th>Kunde</th><th>Betrag</th><th>Status</th><th></th></tr></thead><tbody>${!(data&&data.length)?'<tr><td colspan="6"><div class="em" style="padding:30px"><div class="em-i">💶</div><div class="em-t">Keine Rechnungen</div></div></td></tr>':data.map(r=>`<tr><td style="font-family:monospace;font-size:11px">${r.rechnungsnummer||'–'}</td><td>${r.datum||'–'}</td><td>${r.kunde||'–'}</td><td><b>€${parseFloat(r.betrag||0).toFixed(2)}</b></td><td><span class="bd ${r.status==='bezahlt'?'bd-g':r.status==='offen'?'bd-o':'bd-r'}">${r.status||'offen'}</span></td><td style="white-space:nowrap"><button onclick="mRechEdit('${r.id}',${JSON.stringify(r).replace(/'/g,"&#39;")},${JSON.stringify(proj||[]).replace(/'/g,"&#39;")})" style="background:none;border:none;cursor:pointer;font-size:16px">✏️</button><button onclick="delRech('${r.id}')" style="background:none;border:none;cursor:pointer;color:var(--red);font-size:16px">🗑️</button></td></tr>`).join('')}</tbody></table></div>`);}catch(e){sC(`<div class="em"><div class="em-i">⚠️</div><div class="em-t">${e.message}</div></div>`);}}
function mRech(proj){const nr='RE-'+new Date().getFullYear()+'-'+String(Math.floor(Math.random()*9000)+1000);oM(`<div class="mt">💶 Rechnung erstellen</div><div class="fr"><div class="fg"><label>RECHNUNGSNR.</label><input id="f-nr" value="${nr}"></div><div class="fg"><label>DATUM</label><input id="f-d" type="date" value="${new Date().toISOString().split('T')[0]}"></div></div><div class="fg"><label>PROJEKT</label><select id="f-p"><option value="">–</option>${(proj||[]).map(p=>`<option value="${p.id}">${p.name}</option>`).join('')}</select></div><div class="fg"><label>KUNDE *</label><input id="f-ku" placeholder="Müller GmbH"></div><div class="fg"><label>BETRAG (€) *</label><input id="f-b" type="number" step="0.01" placeholder="0.00"></div><div class="fg"><label>STATUS</label><select id="f-s"><option value="offen">Offen</option><option value="bezahlt">Bezahlt</option><option value="storniert">Storniert</option></select></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveRech('')">💾 Erstellen</button></div>`);}
function mRechEdit(id,r,proj){oM(`<div class="mt">💶 Rechnung bearbeiten</div><div class="fr"><div class="fg"><label>RECHNUNGSNR.</label><input id="f-nr" value="${r.rechnungsnummer||''}"></div><div class="fg"><label>DATUM</label><input id="f-d" type="date" value="${r.datum||''}"></div></div><div class="fg"><label>PROJEKT</label><select id="f-p"><option value="">–</option>${(proj||[]).map(p=>`<option value="${p.id}"${p.id===r.projekt_id?' selected':''}>${p.name}</option>`).join('')}</select></div><div class="fg"><label>KUNDE *</label><input id="f-ku" value="${r.kunde||''}"></div><div class="fg"><label>BETRAG (€) *</label><input id="f-b" type="number" step="0.01" value="${r.betrag||''}"></div><div class="fg"><label>STATUS</label><select id="f-s"><option value="offen"${r.status==='offen'?' selected':''}>Offen</option><option value="bezahlt"${r.status==='bezahlt'?' selected':''}>Bezahlt</option><option value="storniert"${r.status==='storniert'?' selected':''}>Storniert</option></select></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveRech('${id}')">💾 Speichern</button></div>`);}
async function saveRech(id){const ku=document.getElementById('f-ku').value.trim(),b=document.getElementById('f-b').value;if(!ku||!b){alert('Kunde und Betrag fehlen!');return;}const o={rechnungsnummer:document.getElementById('f-nr').value,projekt_id:document.getElementById('f-p').value||null,kunde:ku,datum:document.getElementById('f-d').value,betrag:parseFloat(b),status:document.getElementById('f-s').value};const{error}=id?await sb.from('rechnungen').update(o).eq('id',id):await sb.from('rechnungen').insert(o);if(error){alert('Fehler: '+error.message);return;}cM();pgRech();}
async function delRech(id){if(!confirm('Rechnung löschen?'))return;await sb.from('rechnungen').delete().eq('id',id);pgRech();}
async function mBt(pid){let wetterText='Wird geladen...';try{const pos=await new Promise((r,e)=>navigator.geolocation.getCurrentPosition(r,e));const lat=pos.coords.latitude,lon=pos.coords.longitude;const wr=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current_weather=true`);const wd=await wr.json();const wc=wd.current_weather?.weathercode;const wmap={0:'☀️ Klar',1:'🌤️ Leicht bewölkt',2:'⛅ Bewölkt',3:'☁️ Bedeckt',45:'🌫️ Nebel',51:'🌦️ Nieselregen',61:'🌧️ Regen',71:'❄️ Schneefall',80:'🌧️ Regenschauer',95:'🌩️ Gewitter'};wetterText=`${wmap[wc]||'🌤️ Wechselhaft'}, ${Math.round(wd.current_weather?.temperature||0)}°C`;}catch(e){wetterText='Wetter nicht verfügbar';}
const schnell=['🧱 Maurerarbeiten','🪟 Fenster eingebaut','🔌 Elektroarbeiten','🚿 Sanitärarbeiten','🏗️ Gerüst auf/abgebaut','🚛 Materiallieferung','🔍 Bauabnahme','⚠️ Mangel festgestellt','🪣 Malerarbeiten','🔩 Metallbauarbeiten','🪵 Zimmerarbeiten','🏠 Dacharbeiten'];
oM(`<div class="mt">📋 Bautagesprotokoll</div><div style="background:var(--pl);border-radius:10px;padding:12px;margin-bottom:14px;font-size:13px"><div style="display:flex;justify-content:space-between"><span>📅 ${new Date().toLocaleDateString('de-DE')}</span><span>${wetterText}</span></div></div><div class="fr"><div class="fg"><label>ZEIT VON</label><input id="f-zv" type="time" value="07:00"></div><div class="fg"><label>ZEIT BIS</label><input id="f-zb" type="time" value="16:00"></div></div><div class="fg"><label>GEWERK / FIRMA</label><input id="f-gw" placeholder="z.B. Maurer GmbH"></div><div class="fg"><label>ANWESENDE MITARBEITER</label><input id="f-mit" placeholder="z.B. Max M., Peter S."></div><div class="fg"><label>TÄTIGKEITEN *</label><div style="display:flex;flex-wrap:wrap;gap:6px;margin-bottom:8px">${schnell.map(s=>`<button type="button" onclick="addText('f-desc','${s}')" style="padding:5px 10px;background:var(--pl);color:var(--pd);border:none;border-radius:20px;font-size:11px;font-weight:700;cursor:pointer;font-family:inherit">${s}</button>`).join('')}</div><textarea id="f-desc" rows="4" placeholder="Beschreibung der Tätigkeiten..."></textarea></div><div class="fg"><label>VERWENDETES MATERIAL</label><textarea id="f-mat" rows="2" placeholder="z.B. 50 Sack Zement..."></textarea></div><div class="fg"><label>LIEFERUNGEN</label><input id="f-lief" placeholder="z.B. Holzlieferung Firma XY"></div><div class="fg"><label>MÄNGEL / BESONDERHEITEN</label><textarea id="f-maeng" rows="2" placeholder="z.B. Riss in Wand entdeckt..."></textarea></div><div class="fg"><label>📸 FOTOS</label><div class="photo-up" onclick="document.getElementById('bt-fi').click()" style="padding:12px"><input type="file" id="bt-fi" accept="image/*" multiple style="display:none" onchange="previewBtFotos()"><div style="font-size:18px">📷</div><div style="font-size:12px;font-weight:700">Fotos hinzufügen</div></div><div id="bt-fp" class="photo-prev"></div></div><div class="fg"><label>✍️ UNTERSCHRIFT BAULEITER</label><canvas id="sig-bl" width="400" height="120" style="border:1.5px solid var(--border);border-radius:9px;background:#fff;touch-action:none;width:100%"></canvas><button type="button" onclick="clearSig('sig-bl')" style="margin-top:4px;padding:4px 10px;background:var(--bg);border:1px solid var(--border);border-radius:6px;font-size:11px;cursor:pointer;font-family:inherit">🗑️ Löschen</button></div><div class="fg"><label>✍️ UNTERSCHRIFT AUFTRAGGEBER</label><canvas id="sig-ag" width="400" height="120" style="border:1.5px solid var(--border);border-radius:9px;background:#fff;touch-action:none;width:100%"></canvas><button type="button" onclick="clearSig('sig-ag')" style="margin-top:4px;padding:4px 10px;background:var(--bg);border:1px solid var(--border);border-radius:6px;font-size:11px;cursor:pointer;font-family:inherit">🗑️ Löschen</button></div><div class="ma"><button class="bc" onclick="cM()">Abbrechen</button><button class="bs" onclick="saveBt('${pid}','${wetterText}')">💾 Speichern</button></div>`);initSig('sig-bl');initSig('sig-ag');}
function addText(id,txt){const el=document.getElementById(id);el.value+=(el.value?'\n':'')+txt;}
function initSig(id){const c=document.getElementById(id);if(!c)return;const ctx=c.getContext('2d');let drawing=false;const getPos=(e)=>{const r=c.getBoundingClientRect();const t=e.touches?e.touches[0]:e;return{x:(t.clientX-r.left)*(c.width/r.width),y:(t.clientY-r.top)*(c.height/r.height)};};c.addEventListener('mousedown',e=>{drawing=true;const p=getPos(e);ctx.beginPath();ctx.moveTo(p.x,p.y);});c.addEventListener('mousemove',e=>{if(!drawing)return;const p=getPos(e);ctx.lineTo(p.x,p.y);ctx.strokeStyle='#0f172a';ctx.lineWidth=2;ctx.lineCap='round';ctx.stroke();});c.addEventListener('mouseup',()=>drawing=false);c.addEventListener('touchstart',e=>{e.preventDefault();drawing=true;const p=getPos(e);ctx.beginPath();ctx.moveTo(p.x,p.y);},{passive:false});c.addEventListener('touchmove',e=>{e.preventDefault();if(!drawing)return;const p=getPos(e);ctx.lineTo(p.x,p.y);ctx.strokeStyle='#0f172a';ctx.lineWidth=2;ctx.lineCap='round';ctx.stroke();},{passive:false});c.addEventListener('touchend',()=>drawing=false);}
function clearSig(id){const c=document.getElementById(id);c.getContext('2d').clearRect(0,0,c.width,c.height);}
function previewBtFotos(){const files=document.getElementById('bt-fi').files;const prev=document.getElementById('bt-fp');prev.innerHTML='';for(const f of files){const r=new FileReader();r.onload=e=>{const i=document.createElement('img');i.src=e.target.result;i.className='pt-img';i.onclick=()=>window.open(e.target.result,'_blank');i.style.cursor='pointer';prev.appendChild(i);};r.readAsDataURL(f);}}
async function saveBt(pid,wetter){const d=document.getElementById('f-desc').value.trim();if(!d){alert('Beschreibung fehlt!');return;}const sigBl=document.getElementById('sig-bl').toDataURL();const sigAg=document.getElementById('sig-ag').toDataURL();const{error}=await sb.from('bautagebuch').insert({projekt_id:pid,datum:new Date().toISOString().split('T')[0],beschreibung:document.getElementById('f-desc').value,wetter:wetter,gewerk:document.getElementById('f-gw').value,mitarbeiter:document.getElementById('f-mit').value,material_verwendet:document.getElementById('f-mat').value,lieferungen:document.getElementById('f-lief').value,maengel:document.getElementById('f-maeng').value,zeit_von:document.getElementById('f-zv').value,zeit_bis:document.getElementById('f-zb').value,unterschrift_bauleiter:sigBl,unterschrift_auftraggeber:sigAg,erstellt_von:me.email});if(error){alert('Fehler: '+error.message);return;}cM();pgProjDetail(pid);}
function pgEinst(){sC(`<div class="ph"><div><div class="pt">⚙️ Einstellungen</div><div class="ps">Firma & App konfigurieren</div></div></div><div class="tc" style="margin-bottom:20px"><div class="th"><span class="th-t">🏢 Firmendaten</span></div><div style="padding:18px"><div class="fg"><label>FIRMENNAME</label><input id="s-firma" value="${settings.firma||''}" placeholder="Alem Facility GmbH"></div><div class="fg"><label>ADRESSE</label><input id="s-adr" value="${settings.adresse||''}" placeholder="Musterstr. 1, Berlin"></div><div class="fr"><div class="fg"><label>TELEFON</label><input id="s-tel" value="${settings.telefon||''}" placeholder="+49 ..."></div><div class="fg"><label>E-MAIL</label><input id="s-em" value="${settings.email||''}" placeholder="info@firma.de"></div></div></div></div><div class="tc" style="margin-bottom:20px"><div class="th"><span class="th-t">💶 Rechnungseinstellungen</span></div><div style="padding:18px"><div class="fr"><div class="fg"><label>MWST (%)</label><input id="s-mwst" type="number" value="${settings.mwst||19}" placeholder="19"></div><div class="fg"><label>ZAHLUNGSZIEL (TAGE)</label><input id="s-ziel" type="number" value="${settings.zahlungsziel||14}" placeholder="14"></div></div><div class="fr"><div class="fg"><label>UST-ID</label><input id="s-ustid" value="${settings.ustid||''}" placeholder="DE123456789"></div><div class="fg"><label>STEUERNUMMER</label><input id="s-steuernr" value="${settings.steuernr||''}" placeholder="123/456/78901"></div></div><div class="fg"><label>IBAN</label><input id="s-iban" value="${settings.iban||''}" placeholder="DE89 3704 0044 0532 0130 00"></div><div class="fg"><label>BANK</label><input id="s-bank" value="${settings.bank||''}" placeholder="Deutsche Bank"></div></div></div><div class="tc" style="margin-bottom:20px"><div class="th"><span class="th-t">👤 Konto</span></div><div style="padding:18px"><div class="fg"><label>E-MAIL</label><input value="${me?.email||''}" disabled style="background:#f0f4f8;color:var(--muted)"></div><div class="fg"><label>BENUTZER-ID</label><input value="${me?.id||''}" disabled style="background:#f0f4f8;color:var(--muted);font-size:11px;font-family:monospace"></div></div></div><button class="bp" style="width:100%;padding:14px;font-size:15px" onclick="saveSettings()">💾 Einstellungen speichern</button>`);}
function exportBtPDF(bt,projektName,firmaName){
const{jsPDF}=window.jspdf;
const doc=new jsPDF();
const blau=[26,86,219];
const grau=[100,116,139];
doc.setFillColor(...blau);
doc.rect(0,0,210,35,'F');
doc.setTextColor(255,255,255);
doc.setFontSize(18);doc.setFont('helvetica','bold');
doc.text('Bautagesprotokoll',14,15);
doc.setFontSize(10);doc.setFont('helvetica','normal');
doc.text(firmaName||'',14,23);
doc.text(`Projekt: ${projektName||''}`,14,30);
doc.setTextColor(0,0,0);
let y=45;
doc.setFillColor(240,244,248);
doc.rect(14,y,182,22,'F');
doc.setFontSize(10);doc.setFont('helvetica','bold');
doc.setTextColor(...blau);
doc.text('PROTOKOLL-INFORMATIONEN',16,y+7);
doc.setFont('helvetica','normal');doc.setTextColor(0,0,0);
doc.text(`Datum: ${bt.datum||'–'}`,16,y+14);
doc.text(`Zeit: ${bt.zeit_von||'–'} – ${bt.zeit_bis||'–'} Uhr`,80,y+14);
doc.text(`Wetter: ${bt.wetter||'–'}`,16,y+20);
doc.text(`Erstellt von: ${bt.erstellt_von||'–'}`,80,y+20);
y+=30;
const sections=[
{title:'👷 GEWERK / FIRMA',value:bt.gewerk},
{title:'👥 ANWESENDE MITARBEITER',value:bt.mitarbeiter},
{title:'🔨 DURCHGEFÜHRTE TÄTIGKEITEN',value:bt.beschreibung},
{title:'🧱 VERWENDETES MATERIAL',value:bt.material_verwendet},
{title:'🚛 LIEFERUNGEN',value:bt.lieferungen},
{title:'⚠️ MÄNGEL / BESONDERHEITEN',value:bt.maengel},
];
sections.forEach(s=>{
if(!s.value)return;
if(y>250){doc.addPage();y=20;}
doc.setFont('helvetica','bold');doc.setTextColor(...blau);doc.setFontSize(9);
doc.text(s.title,14,y);y+=5;
doc.setFont('helvetica','normal');doc.setTextColor(0,0,0);doc.setFontSize(10);
const lines=doc.splitTextToSize(s.value,180);
lines.forEach(l=>{if(y>270){doc.addPage();y=20;}doc.text(l,14,y);y+=6;});
y+=4;
});
if(bt.unterschrift_bauleiter&&bt.unterschrift_bauleiter.length>100){
if(y>220){doc.addPage();y=20;}
doc.setFont('helvetica','bold');doc.setTextColor(...blau);doc.setFontSize(9);
doc.text('✍️ UNTERSCHRIFT BAULEITER',14,y);y+=5;
try{doc.addImage(bt.unterschrift_bauleiter,'PNG',14,y,80,25);}catch(e){}
y+=30;
}
if(bt.unterschrift_auftraggeber&&bt.unterschrift_auftraggeber.length>100){
if(y>220){doc.addPage();y=20;}
doc.setFont('helvetica','bold');doc.setTextColor(...blau);doc.setFontSize(9);
doc.text('✍️ UNTERSCHRIFT AUFTRAGGEBER',14,y);y+=5;
try{doc.addImage(bt.unterschrift_auftraggeber,'PNG',14,y,80,25);}catch(e){}
y+=30;
}
doc.setFontSize(8);doc.setTextColor(...grau);
doc.text(`Erstellt mit Alem Facility PRO | ${new Date().toLocaleDateString('de-DE')}`,14,285);
doc.save(`Bautagesprotokoll_${bt.datum||'heute'}.pdf`);
}

sb.auth.getSession().then(({data:{session}})=>{if(session)sA(session.user);});
